# docs/knowledge/

Baseline de arquitetura, modelo de domínio e fluxos de negócio do
`tear-v2-app` (Laravel + React) — apoio ao desenvolvimento, não é a fonte
oficial de estado do projeto (essa é `docs/_workspace/TASK_ROUTER.md`, ver
`CLAUDE.md`).

## Estrutura

- **`sistema-b/`** — `ARCHITECTURE.md`, `DOMAIN_MODEL.md`,
  `BUSINESS_FLOWS.md`: arquitetura, modelo de domínio e fluxos de negócio
  do `tear-v2-app`.
